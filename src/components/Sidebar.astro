---
const currentPath = Astro.url.pathname;

interface NavItem {
	id: string;
	label: string;
	icon: string;
	href: string;
}

const navItems: NavItem[] = [
	{
		id: 'principales',
		label: 'Principales',
		icon: '🍔',
		href: '/'
	},
	{
		id: 'compartir',
		label: 'Compartir',
		icon: '🍟',
		href: '/picoteo'
	},
	{
		id: 'bebidas',
		label: 'Bebidas',
		icon: '🍺',
		href: '/bebidas'
	},
	{
		id: 'postres',
		label: 'Postres',
		icon: '🍰',
		href: '/postres'
	}
];
---

<aside class="hidden lg:block fixed left-0 top-28 bottom-0 w-64 bg-black/95 backdrop-blur-md border-r border-red-600/30 z-40" transition:persist>
	<nav class="flex flex-col h-full py-8 px-4">
		<h2 class="font-burford text-2xl text-red-600 mb-8 px-4">Menú</h2>
		
		<div class="flex flex-col space-y-2">
			{navItems.map((item) => (
				<a
					href={item.href}
					class:list={[
						"flex items-center gap-4 px-4 py-4 rounded-lg transition-all duration-200 font-bratton uppercase tracking-wider text-sm",
						currentPath === item.href 
							? "bg-red-600/20 text-red-600 border-l-4 border-red-600" 
							: "text-gray-400 hover:text-red-500 hover:bg-red-600/10 border-l-4 border-transparent"
					]}
				>
					<span class="text-3xl">{item.icon}</span>
					<span>{item.label}</span>
				</a>
			))}
		</div>
	</nav>
</aside>

<style>
	aside a {
		-webkit-tap-highlight-color: transparent;
		user-select: none;
	}
</style>
